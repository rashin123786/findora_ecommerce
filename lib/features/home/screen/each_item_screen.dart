import 'package:flutter/material.dart';

class EachItemScreen extends StatelessWidget {
  const EachItemScreen({super.key, required this.id});
  final int id;
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
